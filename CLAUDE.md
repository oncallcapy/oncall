# MultiPair Project on PAR

Read `PROJECT_BRIEF.md` before continuing this project. It contains the current
creative direction, verified PAR mechanics, content boundaries, open decisions,
and the handoff state shared with Codex.

Treat all files under `sources/` as read-only synced material.

Do not launch the token, publish a website, post on X, connect a wallet, or
make irreversible on-chain changes without Erdem's explicit approval of the
final concrete result.

When work produces a durable decision, update `PROJECT_BRIEF.md` so Codex can
continue from the same state.
