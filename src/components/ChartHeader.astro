<header class="chart-header">
  <a class="wordmark" href="/" aria-label="ONCALL — Triage">
    <span class="cross" aria-hidden="true">+</span>
    <span>ONCALL<span class="wordmark-note">NIGHT SHIFT / OPEN FILE</span></span>
  </a>
  <p class="header-note meta">Medicine. Science. Market culture.<br /><span>Keep the evidence in the chart.</span></p>
</header>

<style>
  .chart-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1.5rem;
    padding-bottom: 1.75rem;
    color: var(--paper);
  }
  .wordmark {
    display: flex;
    align-items: center;
    gap: 0.875rem;
    font-family: var(--font-display);
    font-size: 2rem;
    font-weight: 800;
    line-height: 1;
    text-decoration: none;
  }
  .wordmark:focus-visible { outline-color: var(--paper); }
  .cross {
    display: grid;
    place-items: center;
    width: 2.5rem;
    height: 2.5rem;
    border: 1px solid currentColor;
    color: var(--coral);
    font-family: var(--font-meta);
    font-size: 2rem;
    font-weight: 400;
  }
  .wordmark-note {
    display: block;
    margin-top: 0.3rem;
    font-family: var(--font-meta);
    font-size: 0.55rem;
    font-weight: 400;
    letter-spacing: 0.09em;
  }
  .header-note { text-align: right; }
  .header-note span { opacity: 0.75; }
  @media (max-width: 47.999rem) {
    .chart-header { align-items: flex-start; padding-bottom: 1.5rem; }
    .header-note { max-width: 11rem; font-size: 0.55rem; }
    .wordmark { font-size: 1.65rem; gap: 0.5rem; }
    .cross { width: 2rem; height: 2rem; }
  }
  @media (max-width: 23rem) {
    .chart-header { flex-wrap: wrap; }
    .header-note { max-width: none; text-align: left; }
  }
</style>
